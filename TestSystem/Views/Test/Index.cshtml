@model IEnumerable<TestSystem.Models.Test>

@{
    ViewData["Title"] = "Список тестов";
}

<h2>Список тестов</h2>
<h3>Общие тесты (тест содержит вопросы из разных разделов)</h3>

@foreach (var item in Model)
{
    <p><pre>    @Html.ActionLink(item.Name, "GeneralTest", new { index = item.Id })</pre></p>
}

<br />
<h3>Тесты по разделам</h3>
<p><pre>    @Html.ActionLink("Орфография", "Category", new { category = "Орфография" })</pre></p>
<p><pre>    @Html.ActionLink("Пунктуация", "Category", new { category = "Пунктуация" })</pre></p>
<p><pre>    @Html.ActionLink("Синтаксис", "Category", new { category = "Синтаксис" })</pre></p>
<p><pre>    @Html.ActionLink("Морфология", "Category", new { category = "Морфология" })</pre></p>

